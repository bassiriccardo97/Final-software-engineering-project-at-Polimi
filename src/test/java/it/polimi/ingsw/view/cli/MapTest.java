package it.polimi.ingsw.view.cli;

import it.polimi.ingsw.connections.Server;
import it.polimi.ingsw.model.card.ammotile.AmmoTile;
import it.polimi.ingsw.model.game.AlphaGame;
import it.polimi.ingsw.model.player.Player;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;

import static org.junit.jupiter.api.Assertions.*;

class MapTest {

    /*
    @Test
    void plotMap(Map map) {
        ArrayList<Player> p = new ArrayList<>();
        AlphaGame g = new AlphaGame(1, p,false, 8);
        Map testMap = new Map(2);
        Player p0 = new Player('b');
        Player p1 = new Player('e');
        Player p2 = new Player('y');
        map.addPlayer(p0, 2);
        map.addPlayer(p1, 2);
        map.addPlayer(p2, 4);

        AmmoTile ammo = new AmmoTile(0, 1, 1, true);
        map.addAmmoTile(0, ammo);
        map.addAmmoTile(1, ammo);
        map.addAmmoTile(3, ammo);
        map.removeAmmoTile(3);
        map.addAmmoTile(4, ammo);
        map.addAmmoTile(11, ammo);
        plotMap(testMap);
    }*/
}